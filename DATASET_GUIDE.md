# 数据集放置指南

## 📁 完整目录结构

```
e:\Big-Data-Security-Assignment\
├── 📄 核心代码
│   ├── main.py                          # 主实验程序
│   ├── quick_test.py                    # 快速测试
│   ├── data_preparation.py              # 数据准备模块
│   ├── denoise_algorithms.py            # 5种降噪算法
│   ├── evaluation.py                    # MCD和WER评估
│   ├── visualize_results.py             # 结果可视化
│   ├── compute.py                       # 评估函数库
│   └── config.py                        # 配置文件
│
├── 📦 依赖与运行
│   ├── requirements.txt                 # Python依赖
│   ├── install_dependencies.bat         # 安装脚本
│   └── run_experiment.bat               # 运行脚本
│
├── 📖 文档
│   └── README.md                        # 项目说明
│
├── 📁 数据集目录 (您需要放置数据的地方)
│   └── data/
│       └── VCTK-Corpus/
│           ├── clean/                   ← 放置 clean_testset_wav.zip 解压后的文件
│           ├── noisy/                   ← 放置 noisy_testset_wav.zip 解压后的文件
│           └── txt/                     ← 放置 testset_txt.zip 解压后的文件
│
├── 📁 实验输出目录 (自动生成)
│   └── output/
│       ├── denoised/                    # 降噪后的音频文件
│       │   ├── spectral_subtraction/
│       │   ├── wiener_filter/
│       │   ├── bandpass_filter/
│       │   ├── deep_learning/
│       │   └── hybrid/
│       ├── plots/                       # 可视化图表
│       ├── evaluation_*.csv             # 各算法评估结果
│       └── algorithm_comparison.csv     # 算法对比结果
│
└── 📁 快速测试目录
    └── test_data/                       # 快速测试用数据
```

## 🎯 数据集放置步骤

### 第一步：下载数据集文件
下载以下3个文件：
- `clean_testset_wav.zip` (147.1MB)
- `noisy_testset_wav.zip` (162.6MB)
- `testset_txt.zip` (429.9KB)

### 第二步：解压并放置
1. 解压 `clean_testset_wav.zip`，将所有 .wav 文件放入：
   ```
   data\VCTK-Corpus\clean\
   ```

2. 解压 `noisy_testset_wav.zip`，将所有 .wav 文件放入：
   ```
   data\VCTK-Corpus\noisy\
   ```

3. 解压 `testset_txt.zip`，将所有 .txt 文件放入：
   ```
   data\VCTK-Corpus\txt\
   ```

### 第三步：验证文件结构
正确的结构应该是：
```
data\VCTK-Corpus\
├── clean\
│   ├── p232_001.wav
│   ├── p232_002.wav
│   └── ... (更多 .wav 文件)
├── noisy\
│   ├── p232_001.wav
│   ├── p232_002.wav
│   └── ... (更多 .wav 文件)
└── txt\
    ├── p232_001.txt
    ├── p232_002.txt
    └── ... (更多 .txt 文件)
```

## ✅ 验证数据集
运行以下命令验证数据集是否正确放置：
```powershell
python quick_test.py
```

如果看到系统自动检测到VCTK数据集，说明放置正确。

## 🚀 开始实验
数据集放置完成后，运行完整实验：
```powershell
python main.py --num_samples 30 --max_eval 20
```