# 暂停/恢复实验使用指南

## 📋 功能概述

新的暂停/恢复功能专为长时间实验设计，特别适合处理全部824个样本的大规模实验。

### 🎯 核心特性

- **智能暂停**: 每处理指定数量的文件后自动暂停
- **进度保存**: 实验进度实时保存到JSON文件
- **断点恢复**: 从任意暂停点精确恢复实验
- **状态监控**: 随时查看实验进度和状态
- **异常保护**: 即使意外中断也能保留进度

---

## 🚀 使用场景

### 场景1: 中等规模实验 (推荐新手)
```powershell
# 处理100个样本，每50个暂停一次
python pausable_experiment.py --num_samples 100 --batch_size 50 --max_eval 50
```

**适用情况**:
- 初次使用，想要快速体验功能
- 计算资源有限
- 预计运行时间: 1-2小时

### 场景2: 大规模实验 (论文级别)
```powershell
# 使用全部824个样本，每50个暂停一次
python pausable_experiment.py --num_samples 824 --batch_size 50 --max_eval 100
```

**适用情况**:
- 需要权威的实验结果
- 有充足的计算资源
- 预计运行时间: 5-8小时

### 场景3: 超大规模实验 (研究级别)
```powershell
# 全数据集 + 全评估
python pausable_experiment.py --num_samples 824 --batch_size 100 --max_eval 824
```

**适用情况**:
- 研究论文需要
- 最权威的实验结果
- 预计运行时间: 8-12小时

---

## 📊 实验流程示例

### 第一次运行
```powershell
PS E:\Big-Data-Security-Assignment> python pausable_experiment.py --num_samples 200 --batch_size 50

================================================================================
                    语音降噪与增强实验
================================================================================

步骤1: 准备数据集
============================================================
✅ 发现VCTK数据集:
   干净音频: 824 个文件
   含噪音频: 824 个文件
📊 将使用前 200 个样本进行实验

步骤2: 应用降噪算法 (all)
============================================================
📊 总计处理 200 个音频文件
🔧 将运行算法: ['spectral_subtraction', 'wiener_filter', 'bandpass_filter', 'deep_learning', 'hybrid']

🔄 开始处理算法: spectral_subtraction
降噪 (spectral_subtraction): 100%|█████| 50/200 [03:25<00:00,  4.12s/it]

======================================================================
⏸️  暂停点: 已处理 50 个文件
📊 当前进度:
   • 当前算法: spectral_subtraction
   • 已完成算法: 0
   • 总处理文件数: 50
   • spectral_subtraction: 50 个文件 🔄
======================================================================
是否继续实验？ (y=继续, n=暂停并保存): n

🛑 实验已暂停，进度已保存。
📁 进度文件: E:\Big-Data-Security-Assignment\output\experiment_progress.json
🔄 要恢复实验，请运行: python resume_experiment.py
```

### 查看实验状态
```powershell
PS E:\Big-Data-Security-Assignment> python view_status.py

📊 语音降噪实验状态查看
============================================================
📋 实验状态: 🔄 RUNNING
🕐 开始时间: 2025-11-24T15:30:25.123456
📅 最后更新: 2025-11-24T15:33:50.654321

⚙️ 实验配置:
   • 总样本数: 200
   • 目标算法: all
   • 评估文件数: 20
   • 批处理大小: 50 (每批暂停一次)

🔧 算法处理进度:
   • spectral_subtraction: 50 个文件 🔄

📈 统计信息:
   • 总处理文件: 50
   • 已完成算法: 0
   • 当前算法: spectral_subtraction

💡 下一步操作:
   • 实验已暂停，恢复运行: python resume_experiment.py
   • 重新开始实验: python resume_experiment.py (选择重新开始)
============================================================
```

### 恢复实验
```powershell
PS E:\Big-Data-Security-Assignment> python resume_experiment.py

🔄 语音降噪实验恢复程序
==================================================
======================================================================
📋 实验进度信息
======================================================================
🕐 开始时间: 2025-11-24T15:30:25.123456
📅 最后更新: 2025-11-24T15:33:50.654321
📊 实验状态: running

⚙️ 实验配置:
   • 总样本数: 200
   • 算法: all
   • 评估文件数: 20
   • 批处理大小: 50

🔧 算法进度:
   • spectral_subtraction: 50 个文件 🔄

📈 总计处理: 50 个文件
✅ 已完成算法: 0
======================================================================

🤔 选择操作:
1. 恢复实验 (继续之前的进度)
2. 重新开始 (清除进度，从头开始)
3. 取消

请选择 (1/2/3): 1

🔄 恢复实验...
📝 执行命令: python pausable_experiment.py --num_samples 200 --algorithm all --max_eval 20 --batch_size 50 --resume

================================================================================
                    语音降噪与增强实验
                        (恢复模式)
================================================================================

🔄 恢复之前的实验...
📅 开始时间: 2025-11-24T15:30:25.123456
✅ 已完成算法: []
   • spectral_subtraction: 50 个文件

[实验继续...]
```

---

## 🛠️ 参数详解

### pausable_experiment.py 参数
```powershell
python pausable_experiment.py [参数]
```

| 参数 | 默认值 | 说明 | 示例 |
|------|--------|------|------|
| `--num_samples` | 50 | 处理的音频样本数量 | `--num_samples 824` |
| `--algorithm` | all | 使用的降噪算法 | `--algorithm hybrid` |
| `--max_eval` | 20 | 评估的文件数量 | `--max_eval 100` |
| `--batch_size` | 50 | 暂停间隔 (每N个文件) | `--batch_size 100` |
| `--data_root` | ./data | 数据集根目录 | `--data_root /path/to/data` |
| `--output_root` | ./output | 输出根目录 | `--output_root /path/to/output` |
| `--resume` | False | 恢复模式标志 | `--resume` |

### resume_experiment.py 参数
```powershell
python resume_experiment.py [参数]
```

| 参数 | 默认值 | 说明 | 示例 |
|------|--------|------|------|
| `--output_root` | ./output | 输出根目录 | `--output_root /path/to/output` |
| `--auto` | False | 自动恢复，不询问 | `--auto` |

### view_status.py 参数
```powershell
python view_status.py [参数]
```

| 参数 | 默认值 | 说明 | 示例 |
|------|--------|------|------|
| `--output_root` | ./output | 输出根目录 | `--output_root /path/to/output` |

---

## 📂 进度文件说明

进度信息保存在 `output/experiment_progress.json` 中：

```json
{
  "start_time": "2025-11-24T15:30:25.123456",
  "last_update": "2025-11-24T15:33:50.654321",
  "status": "running",
  "completed_algorithms": [],
  "algorithm_progress": {
    "spectral_subtraction": 50,
    "wiener_filter": 0
  },
  "current_algorithm": "spectral_subtraction",
  "total_samples": 200,
  "experiment_config": {
    "num_samples": 200,
    "algorithm": "all",
    "max_eval": 20,
    "batch_size": 50
  }
}
```

---

## ⚠️ 注意事项

### 1. 进度文件保护
- 进度文件自动保存，请勿手动删除
- 如需重新开始，使用 `resume_experiment.py` 的重新开始选项

### 2. 暂停时机
- 只在文件批次完成后暂停，不会中断单个文件的处理
- 可以使用 `Ctrl+C` 强制中断，进度会自动保存

### 3. 磁盘空间
- 全数据集实验需要约2-3GB磁盘空间存储降噪结果
- 确保有足够的磁盘空间

### 4. 内存使用
- 每个音频文件处理完后立即释放内存
- 内存使用相对稳定，不会随文件数量线性增长

---

## 🔧 故障排除

### Q1: 恢复时提示找不到进度文件
**A**: 检查输出目录是否正确，确保 `output/experiment_progress.json` 存在

### Q2: 实验中途异常退出怎么办？
**A**: 运行 `python resume_experiment.py` 即可从最后保存的进度恢复

### Q3: 想要重新开始实验
**A**: 运行 `python resume_experiment.py`，选择"重新开始"选项

### Q4: 如何调整暂停频率？
**A**: 使用 `--batch_size` 参数，数值越大暂停越少

---

## 📈 性能对比

| 实验规模 | 样本数 | 预计时间 | 推荐batch_size | 使用场景 |
|----------|--------|----------|----------------|----------|
| 小规模 | 50 | 30分钟 | 25 | 快速验证 |
| 中规模 | 100 | 1-2小时 | 50 | 一般实验 |
| 大规模 | 500 | 4-6小时 | 100 | 深入研究 |
| 全规模 | 824 | 8-12小时 | 100 | 权威结果 |

选择合适的规模可以在实验质量和时间成本之间取得平衡。